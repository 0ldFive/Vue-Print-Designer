# Table 元素可视化增强 TODO

> 目标：降低 table 元素的手动配置成本，为「手写 JSON」补充可视化操作（JSON 编辑功能保留，二者共存互补）。
> 现状：`TableColumn` 已扩展为 `field/header/width/align/hidden/aggregate/format`（`src/types/index.ts`）；属性面板由 `PropertyTableColumns.vue` 提供列卡片管理器、`PropertyTableFooterRows.vue` 提供表脚行管理器（两者均为可视化 ⇄ JSON 双模式）；已具备合并单元格、列宽拖拽、footer 聚合能力。

## P0 - 可视化编辑器（核心）✅ 已完成

### 1. 列卡片管理器

- [x] 属性面板新增卡片式列列表，与「列配置 JSON」共存（可视化编辑为主入口，JSON 作为高级模式保留）
- [x] 二者双向同步：可视化改动实时反映到 JSON；JSON 改动（合法时）实时反映到卡片列表
- [x] JSON 非法时的容错提示（不覆盖现有 columns，仅在恢复合法后同步）
- [x] 每个列卡片独占一行；卡片常驻单行：拖拽把手 + 表头文本 + 隐藏 checkbox（无文字标签，仅 hover tooltip）+ 齿轮按钮
- [x] 高级项（字段 / 宽度 / 列对齐 / 上移 / 下移 / 复制 / 删除）收进齿轮按钮按卡片独立展开
- [x] 空间不足时字段/表头输入框自动换行（窄面板下不挤压）
- [x] 添加列 / 复制列（复制现有列及其样式）
- [x] 拖拽排序（降级方案：上移/下移按钮）
- [x] 列宽「平均分配」按钮
- [x] 可视化 ⇄ JSON 模式切换入口（标题栏双图标切换）

> 注：「列宽单位 px / 比例」一项未做——渲染时列宽本就按比例缩放到元素宽度（见 `TableElement.vue` 的 `getColumnPixelWidth`），两者等价，做成开关是无意义的选项。

### 2. 字段绑定可视化

- [x] `field` 支持下拉建议（datalist），来源：testData keys / 已注册变量叶子节点 / 数据变量首行字段
- [x] 「从数据推断列」：取数据变量首行字段自动补齐缺失列；无数据变量时回退到元素设计态数据行

### 3. 扩展 TableColumn 数据结构

```ts
export interface TableColumn {
  field: string;
  header: string;
  width: number;
  align?: "left" | "center" | "right";   // 列级对齐
  hidden?: boolean;                       // 设计态保留、渲染/排版隐藏
  aggregate?: "SUM" | "AVG" | "COUNT" | "MIN" | "MAX" | "NONE";
  format?: string;                        // 数字/日期格式化，如 "0.00" / "YYYY-MM-DD"
}
```

- [x] 类型定义扩展
- [x] `align` 接入渲染（表头 / 表体 / 表脚，列级覆盖表级）
- [x] `hidden` 接入渲染（`processedData` 与 `getEffectiveTableColumns` 统一过滤，索引按 field 回写原始列）
- [x] `aggregate` 接入 footer 聚合（替换写死的 SUM，支持 AVG/COUNT/MIN/MAX/NONE）
- [x] 撤销/重做自动接入（`updateElement` 对 `columns` 走 `ELEMENT_CONTENT` 快照）

> `format` 字段已定义但尚未参与渲染，留待后续「单元格格式」需求统一实现。

### 4. 表脚行管理器

- [x] 属性面板「表脚行」由纯 JSON 代码框改为可视化行卡片列表 + JSON 高级模式（双向同步）
- [x] 行卡片常驻单行：拖拽把手 + 行序号（第 N 行）+ 单元格设置按钮
- [x] 展开后按列逐格编辑：显示文本 + 变量字段（与双击表脚单元格弹窗语义一致）
- [x] 添加行 / 复制行 / 删除行 / 拖拽排序（降级：上移行 / 下移行）
- [x] 单元格合并 / 拆分：勾选相邻单元格（支持跨多个表脚行）后点「合并单元格」，已合并单元格提供「拆分单元格」
- [x] 被合并覆盖的单元格自动从编辑器列表隐藏，与渲染/聚合一致；保留其变量绑定，拆分后自动恢复
- [x] 单元格仅含显示文本时回落为字符串，保持 `footerData` JSON 干净
- [x] 隐藏列在编辑器中一并过滤，与渲染保持一致

## P1 - 数据与单元格操作

### 5. 设计态行管理

- [ ] 表格右键 / 面板按钮：插入示例行、删除行、清空数据
- [ ] 数据预览切换：测试数据 ↔ 空骨架（仅表头），与 `designOmitRows` 打通
- [ ] 行高拖拽（复用列宽拖拽实现）或表体行高数值配置

### 6. 单元格增强

- [ ] 双击单元格行内编辑（表头文本/示例值，替代改 JSON）
- [ ] footer 单元格聚合方式可视化：选中 footer 单元格时属性面板出现聚合下拉（复用已就绪的 `aggregate` 字段，目前仅能改列配置 JSON）
- [ ] 单元格格式刷：复制单元格样式（背景/对齐/字体）到其他单元格
- [ ] 单元格背景色快捷设置（当前 style 仅有表级粒度）

## P1-P2 - 样式与主题

### 7. 样式预设与分组

- [ ] 表格样式预设：斑马纹、边框风格（全框/内框/三线表）、表头配色一键套用
- [ ] 奇偶行背景色（斑马纹）配置
- [ ] 表头/表体/表尾行高独立配置（当前仅有整体高度和 footerHeight）
- [ ] 现有 header/footer 颜色字号等散落配置收拢为「表头样式 / 表体样式 / 表尾样式」三个分组

## P2 - 打印分页增强

### 8. 分页能力（基于已有 autoPaginate/tfootRepeat）

- [ ] 每页固定行数选项
- [ ] 每页小计 + 末页总计 footer 模式（仓库/发票场景）
- [ ] 首页与续页使用不同表头（复杂场景，后置）

## P2-P3 - 高级特性（可先不做）

- [ ] 多级表头 / 列分组（`columns` 需支持 children，数据结构改动大）
- [ ] 表达式列：如 `colA - colB`、单价 × 数量
- [ ] 条件样式：值超阈值标红（库存预警场景）
- [ ] 列固定（打印场景意义不大，优先级最低）

---

## 推荐实施路线

1. ~~**第一步**：列卡片管理器 + 表脚行管理器（均含 JSON 共存双向同步）+ 字段下拉绑定 + 从数据推断列（P0）~~ ✅ 已完成
2. **第二步**：设计态行管理 + 双击编辑单元格 + 聚合下拉（P1）
3. **第三步**：样式预设与分页增强（P1-P2）
